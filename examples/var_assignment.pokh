age:= 12.5
// this
stdout("Hello", age)    // comment

y := [1, 2, 3]
x := 1

x := x * 3

myfunc(x, y) {
    m := sum(x,y)
    j := mod(x, y)
    stdout(m, j)
}

myfunc(10, 3)

a := 0

while a < 5 {
    stdout(a)
    a := 6
}
